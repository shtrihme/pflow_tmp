# Вендоренный MCP-бандл коммитится прямо в репозиторий. Помечаем его binary,
# чтобы git не нормализовал EOL и не пытался показывать diff на сотни строк
# минифицированного кода в каждом PR, где обновился бандл.
.pflow/mcp/pflow-mcp.cjs binary
